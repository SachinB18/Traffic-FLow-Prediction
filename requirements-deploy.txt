# Lightweight requirements for deployment
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.6.0

# PyTorch CPU (remove CUDA to reduce image size)
# For GPU, use torch with cuda support
torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu
torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cpu

# Data processing (minimal)
numpy>=2.0
pandas>=2.0
scikit-learn>=1.5

# For Azure (optional)
azure-identity
